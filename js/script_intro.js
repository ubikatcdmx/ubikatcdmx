document.addEventListener('DOMContentLoaded', () => {
    // Redirige después de 5 segundos (5000 milisegundos)
    setTimeout(() => {
        window.location.href = 'views/menu.html'; // Cambia 'menu.html' por la URL de tu menú
    }, 5000);

});
